


import React from 'react';

// Define the type alias for the props
// type Propss = {
//     name: string;
//     age: number;
// };

// Define the functional component
const Header : React.FC = () => {
    return (
        <div>
            heloo
        </div>
    );
};

export default Header ;
